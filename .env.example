# Shared frontend + backend environment
# Copy this file to .env and edit values per environment.

# Frontend (Vite)
VITE_API_ORIGIN=http://localhost:8787
VITE_STUDIO_PASSWORD=shubh123
VITE_API_TIMEOUT_MS=10000

# Backend (Express)
API_PORT=8787
STUDIO_PASSWORD=shubh123

# SMTP transport mode (server-side mail)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=
